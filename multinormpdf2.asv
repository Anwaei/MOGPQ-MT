function p = multinormpdf2(x1, x2, m, P)

m1 = m(1);
m2 = m(2);
Pinv = inv(P);
p11 = P(1,1);
p12 = P(1,2);
p21 = P(2,1);
p22 = P(2,2);

t = -1/2 * (x-m)' /P * (x-m);
p = (1/(2*pi)^(D/2)) * (1/sqrt(det(P))) * exp(t);

end